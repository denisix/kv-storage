# KV Storage Server Configuration Example
# Copy this file to .env and customize the values

# Required: Authentication token for API access
TOKEN=your-secret-token-here

# Optional: Database path (default: ./kv_db)
# DB_PATH=./kv_db

# Optional: Bind address (default: 0.0.0.0:3000)
# BIND_ADDR=0.0.0.0:3000

# Optional: Compression level 1-22 (default: 1)
# Higher = better compression but slower
# COMPRESSION_LEVEL=3

# Optional: Cache capacity (default: 1GB)
# Supports K/M/G suffixes or raw bytes
# Examples: 256M, 1G, 512000000
# CACHE_CAPACITY=1G

# Optional: Flush interval in milliseconds (default: OS managed)
# Set to None for OS-managed flushing (better performance)
# Set to a value like 5000 for periodic flushes every 5 seconds
# FLUSH_INTERVAL_MS=5000

# Optional: Log level (default: info)
# RUST_LOG=info
